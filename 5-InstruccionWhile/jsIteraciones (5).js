function mostrar()
/*

var sexo = prompt("ingrese f ó m .");
 

while ((sexo != "f" ) || (sexo != "m")){
    
    sexo = prompt ("ingrese f o m .gracias");

}


document.getElementById('Sexo').value=sexo;

}*/ //FIN DE LA FUNCIÓN
{

    var numero; // Valor inicial que permite entrar en el bucle
while ((numero !== "f") || (numero !== "m")) {

    numero = prompt("Para terminar el bucle, introduce un número entre 50 y 100:");
}

}