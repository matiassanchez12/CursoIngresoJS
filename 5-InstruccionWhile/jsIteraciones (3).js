function mostrar()
{

var clave = prompt("ingrese el número clave.");

    while (clave != "utn750"){

        alert ("la clave es incorrecta");

        clave = prompt ("ingrese nuevamente el numero clave.");

    }
}//FIN DE LA FUNCIÓN
